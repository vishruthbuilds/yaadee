{
  "buildCommand": "cd yaadee-frontend && npm install && npm run build",
  "outputDirectory": "yaadee-frontend/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
